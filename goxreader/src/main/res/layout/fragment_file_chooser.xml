<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/light_gray_2"
                android:paddingLeft="@dimen/library_elements_padding"
                android:paddingRight="@dimen/library_elements_padding"
                tools:context="com.goxapps.goxreader.filechooser.ui.ExtractedFilesFragment">

    <TextView
        android:id="@+id/textViewNoResults"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:padding="20dp"
        android:text="there are no PDF files on your device"
        android:visibility="gone"/>

    <ProgressBar
        android:id="@+id/loadingFilesProgress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recyclerExtractedFiles"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</RelativeLayout>
